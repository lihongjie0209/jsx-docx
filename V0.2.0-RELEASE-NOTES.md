# jsx-docx v0.2.0 Release Notes

**发布日期**: 2024-12-08  
**版本**: 0.2.0  
**上一版本**: 0.1.0

---

## 🎉 主要功能

### 1. 标准输入支持 (`--stdin`)

现在可以从管道或标准输入直接读取 JSX 内容，无需创建临时文件。

**使用示例：**

```bash
# 从文件管道读取
cat template.jsx | java -jar jsx-docx-0.2.0-fat.jar --stdin -o output.docx

# 从命令行直接生成
echo '<Document><Section><Paragraph><Text>Hello</Text></Paragraph></Section></Document>' | \
  java -jar jsx-docx-0.2.0-fat.jar --stdin -o output.docx

# 结合其他工具使用
curl https://example.com/template.jsx | java -jar jsx-docx-0.2.0-fat.jar --stdin
```

**应用场景：**
- 脚本自动化
- CI/CD 管道集成
- 动态文档生成
- 临时文档创建

---

### 2. 批量转换进度条

批量转换时自动显示进度条，清晰展示转换进度。

**功能特性：**
- 自动启用（批量模式下）
- 显示进度条、百分比、当前文件名
- 与 verbose 模式互不冲突
- 可通过 `--no-progress` 禁用

**示例输出：**
```
Converting: [==========>           ] 5/10 (50%) - test-table.jsx
```

**命令示例：**
```bash
# 默认启用进度条
java -jar jsx-docx-0.2.0-fat.jar examples/*.jsx -d output

# 禁用进度条
java -jar jsx-docx-0.2.0-fat.jar examples/*.jsx -d output --no-progress
```

---

### 3. JSON 报告生成 (`--report`)

生成详细的 JSON 格式转换报告，记录每个文件的转换结果。

**报告内容：**
```json
{
  "total": 10,
  "success": 9,
  "failed": 1,
  "results": [
    {
      "file": "test.jsx",
      "status": "success",
      "output": "output/test.docx",
      "error": null,
      "time_ms": 234
    },
    {
      "file": "broken.jsx",
      "status": "error",
      "output": null,
      "error": "Syntax error at line 5",
      "time_ms": 45
    }
  ]
}
```

**使用示例：**
```bash
# 生成报告
java -jar jsx-docx-0.2.0-fat.jar examples/*.jsx -d output --report report.json

# 查看报告
cat report.json | jq '.'
```

**应用场景：**
- CI/CD 集成和监控
- 自动化测试验证
- 批量处理审计
- 错误追踪和分析

---

## 🔧 改进和优化

### 命令行界面
- 更清晰的错误输出
- 批量转换显示总耗时
- 改进的帮助信息

### 用户体验
- 进度条与其他输出模式良好协作
- stdin 模式使用 stderr 输出日志
- 更友好的错误提示

---

## 📖 文档更新

### 新增文档
- **CHANGELOG.md**: 完整的版本更新历史
- **V0.2.0-RELEASE-NOTES.md**: 本发布说明（当前文档）
- **test-v0.2.0-features.sh**: 功能测试脚本

### 更新文档
- **README.md**: 添加 v0.2.0 功能说明和示例
- **ROADMAP.md**: 标记 v0.2.0 任务为已完成
- **TODO.md**: 移动已完成任务到归档区域

---

## 🚀 快速开始

### 构建

```bash
mvn clean package
```

生成的文件：`target/jsx-docx-0.2.0-fat.jar`

### 基本使用

```bash
# 单文件转换
java -jar target/jsx-docx-0.2.0-fat.jar test.jsx

# 从标准输入
cat template.jsx | java -jar target/jsx-docx-0.2.0-fat.jar --stdin -o output.docx

# 批量转换（带进度条）
java -jar target/jsx-docx-0.2.0-fat.jar examples/*.jsx -d output

# 生成报告
java -jar target/jsx-docx-0.2.0-fat.jar examples/*.jsx -d output --report report.json
```

### 查看帮助

```bash
java -jar target/jsx-docx-0.2.0-fat.jar --help
```

---

## 🔄 从 v0.1.0 升级

### 破坏性变更
无破坏性变更。v0.2.0 完全向后兼容 v0.1.0。

### 新参数
- `--stdin`: 从标准输入读取
- `--progress`: 显示进度条（默认启用）
- `--no-progress`: 禁用进度条
- `--report <file>`: 生成 JSON 报告

### 建议更新
1. 更新构建脚本中的版本号
2. 考虑在 CI/CD 中使用 `--report` 参数
3. 利用 `--stdin` 简化管道操作

---

## 📊 性能指标

| 指标 | v0.1.0 | v0.2.0 | 改进 |
|------|--------|--------|------|
| 单文件转换 | ~2s | ~2s | - |
| 批量转换反馈 | 无进度 | 实时进度条 | ✅ |
| 报告功能 | 无 | 完整 JSON 报告 | ✅ |
| 管道支持 | 无 | 完整支持 | ✅ |

---

## 🐛 已知问题

无新的已知问题。

---

## 🔮 下一步计划（v0.3.0）

根据 [ROADMAP.md](ROADMAP.md)，v0.3.0 将重点关注：

1. **JAR 包体积优化**: 120MB → 40-60MB（平台特定构建）
2. **MCP HTTP 服务器**: 实现 HTTP/SSE 模式
3. **Docker 支持**: 发布官方 Docker 镜像
4. **npm 包装器**: 发布到 npm 仓库
5. **错误提示改进**: 更友好的错误消息

---

## 🙏 致谢

感谢所有为 jsx-docx 做出贡献的人！

---

## 📞 反馈

如有问题或建议，请：
- 📧 提交 [GitHub Issue](https://github.com/lihongjie0209/jsx-docx/issues)
- 💬 参与 [GitHub Discussions](https://github.com/lihongjie0209/jsx-docx/discussions)

---

**完整更新日志**: [CHANGELOG.md](CHANGELOG.md)  
**项目路线图**: [ROADMAP.md](ROADMAP.md)  
**贡献指南**: [CONTRIBUTING.md](CONTRIBUTING.md)
